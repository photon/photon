language: php

php:
  - 5.5

before_script:
  - sudo locale-gen fr_FR fr_FR.UTF-8
  - locale -a  
  - echo "extension = zmq.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - printf "yes\nyes\nno\nyes\n" | pecl install pecl_http
  - composer self-update
  - mkdir test
  - cd test
  - echo "eyJtaW5pbXVtLXN0YWJpbGl0eSI6ICJkZXYifQo=" | base64 -d  > composer.json
  - composer require "photon/photon:dev-php_http-2.x"
  
script: php vendor/photon/photon/src/photon.php selftest

matrix:
  fast_finish: true
