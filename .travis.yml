language: php

sudo: false
dist: precise

php:
  - '5.6'
  - '7.0'
  - '7.1'

matrix:
  fast_finish: true

before_script:
  - ./scripts/travis
  - composer init --no-interaction --stability=dev
  - composer require "photon/photon:dev-master"
  - composer require "phpunit/phpunit:~5.0"
  
script: php vendor/photon/photon/src/photon.php selftest
